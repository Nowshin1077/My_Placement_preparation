#include<bits/stdc++.h>
using namespace std;
int main()

{ long long A, B, C;
  cin >> A >> B >> C;
  if (A > B)
    cout << A << endl;
  else if (B>C)
    cout << B << endl;
  else
    cout << C << endl;
  return 0;
}
